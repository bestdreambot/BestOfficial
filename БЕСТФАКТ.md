# БЕСТФАКТ

Только подтверждённые факты о проекте BestOfficial. Без оценок, без домыслов — если что-то не
проверено, тут этого нет. Каждый релиз — отдельным блоком, факты со ссылкой на изменение.

## Правило (29.07.2026, обновлено 07.08.2026, обновлено 08.08.2026)

С Релиза 4 подпись «Релиз N» под тором показывается и на публичной странице
(bestofficial.vercel.app) — прямое решение Андрея («релиз видят все»). С Релиза 4.3 карточка
«Версия» убрана целиком — больше нет отдельного скрытого документа, есть только один «Релиз»,
метка которого растёт дробно между реальными деплоями (4.2 → 4.21 → 4.3 → …). Локальная
мастерская — `127.0.0.1:2000` (не 8770, см. `CLAUDE.md`).

## Официальный релиз 4.4 — 08.08.2026

Публичная страница обновлена через `git push` в `github.com/bestdreambot/BestOfficial` (main,
коммит [`5bcf2e4`](https://github.com/bestdreambot/BestOfficial/commit/5bcf2e4)) — следом
задеплоено на Vercel через `vercel --prod` из `.deploy-bestofficial/`.

- **Презентация карточки «Релиз»/BestOfficial реорганизована по темам** — Андрей: «то, что ты
  пишешь в презентации — это хронология, тоже круто, но нужна презентация, организуй и
  структурируй». Был плоский список фактов по порядку добавления, стало 5 разделов: Что это / Как
  выглядит / Как пользоваться / Живые данные / Надёжность.
- Строка «Идёт подготовка к Релизу 5» убрана из презентации (статус процесса, не факт о продукте —
  и так виден в баннере готовности рядом).
- Число звёзд/проектов в разделе «Живые данные» считается от реальных данных
  (`PARTICIPANT_CHAIN.length`/`PROJECTS.length`), не захардкожено — не протухнет молча.

## Официальный релиз 4.31 — 08.08.2026

Публичная страница обновлена через `git push` в `github.com/bestdreambot/BestOfficial` (main,
коммит [`2f5afd4`](https://github.com/bestdreambot/BestOfficial/commit/2f5afd4)) — следом
задеплоено на Vercel через `vercel --prod` из `.deploy-bestofficial/`.

- **Moon переставлен** в цепочке проектов — теперь перед Grand Show (было после Exclusive Man).
- **Кликабельные кнопки на карточках**: бейдж «Звезда Best», факты «Руководитель X»/«Помогает в
  проекте X», TikTok-ник в презентации — теперь настоящие кнопки, сразу открывают карточку/тор
  нужного человека или проекта (`jumpToParticipant`/`jumpToProject` в `js/best-app.js`).
- **Дима** — добавлены факты «Помогает в проекте Aleorix» и «Помогает в проекте Anibrox» (кнопки),
  плюс красный статус «Нужно больше информации».
- **Ира** — прислала свою разукрашенную фотографию (арт-проект Moon, где участники разукрашивают
  себе лицо) — поставлена как аватар в её тор (`photos/ira.jpg`), исходник — плотный кадр без
  шеи/плеч, поэтому фото показывается целиком без обрезки (`photoFit:'contain'`). Факт — и на её
  собственной карточке, и на карточке Moon.
- **Библиотека карточек по кругу**: клик по medium-тору внутри открытой карточки листает все
  карточки подряд — сначала все звёзды, потом все проекты, по кругу.
- **Кнопка «назад»** (вверху слева, рядом с крестиком) — настоящая история переходов (как в
  браузере), не просто «предыдущая карточка по списку»: возвращает именно туда, откуда реально
  пришли.

## Официальный релиз 4.3 — 08.08.2026

Ещё один закрепляющий момент — не полный Релиз 5 (световой элемент тора BestOfficial всё ещё не
найден, единственный открытый вопрос). Публичная страница обновлена через `git push` в
`github.com/bestdreambot/BestOfficial` (main, коммит
[`c90e7dd`](https://github.com/bestdreambot/BestOfficial/commit/c90e7dd)) — следом задеплоено на
Vercel через `vercel --prod` из `.deploy-bestofficial/`. Источник — рабочая мастерская
`freex/bestofficial/freexofficial.html` (локальный порт 2000).

- **Карточка «Версия» удалена целиком** — Андрей: «я не пойму от куда ошибки... версию убери,
  работаем с релизом». Два параллельных документа (Релиз + Версия) путали, откуда берутся
  расхождения. Теперь один документ — `RELEASE_INFO` в `js/best-app.js`, с полем `session`
  (короткий якорь «что сделано с прошлого релиза»), которое раньше было только в Версии. Метка
  под тором растёт дробно между деплоями вместо отдельного счётчика.
- **Exclusive Man** — TikTok-адрес был с опечаткой (`best_exlusive.man`, ссылка не открывалась),
  исправлен на подтверждённый Андреем `best_exclusive.man`.
- **Moon** — новый цвет: сине-фиолетовый, ровно между сиреневым (lavender) и синим (news) — по
  прямому слову Андрея («найди цвет между сиреневым и синим»), был бледно-голубой.

## Официальный релиз 4.2 — 08.08.2026

Ещё один закрепляющий момент, тем же принципом, что и 4.1 — не полный Релиз 5 (световой элемент
тора BestOfficial всё ещё не найден, единственный открытый вопрос). Публичная страница обновлена
через `git push` в `github.com/bestdreambot/BestOfficial` (main, коммит
[`c1c603d`](https://github.com/bestdreambot/BestOfficial/commit/c1c603d)) — следом задеплоено на
Vercel через `vercel --prod` из `.deploy-bestofficial/`. Источник — рабочая мастерская
`freex/bestofficial/freexofficial.html` (локальный порт 2000).

- **Valmont переименован в Rush** — по настоянию Олеси, подтверждено Андреем («да будет так»).
  Переименовано везде: слово/aria в `data-projects.js`, ключ палитры в `data-worlds.js`
  (`valmont`→`rush`), роль Олеси в `data-participants.js` (руководитель Rush + Rock Party),
  история переименования — в `PROJECT_FACTS['Rush']`.
- TikTok-адрес проекта **не менялся** (`@best.valmont.man`) — переименование внутри продукта не
  значит автоматическую смену внешнего аккаунта, это отдельный вопрос, не решённый.
- Записано постоянным правилом в `CLAUDE.md` («How Claude works here») — чтобы переименование не
  откатилось случайно и не спуталось со старыми архивными файлами (те помечены как устаревшие, не
  переписаны целиком).
- Проверено на живом проде после деплоя: `https://bestofficial.vercel.app` отвечает (код 200),
  показывает «Релиз 4.2», отдаёт `Rush` во всех трёх data-файлах.

## Официальный релиз 4.1 — 07.08.2026, вечер

«Закрепляющий момент» (прямое слово Андрея) — не полноценный Релиз 5 (два открытых вопроса ниже не
решены), а промежуточная точка фиксации всего, что уже готово. Публичная страница обновлена через
`git push` в `github.com/bestdreambot/BestOfficial` (main, коммит
[`5c96d93`](https://github.com/bestdreambot/BestOfficial/commit/5c96d93)) — следом задеплоено на
Vercel через `vercel --prod` из `.deploy-bestofficial/`. Источник — рабочая мастерская
`freex/bestofficial/freexofficial.html` (локальный порт 2000).

- Два реальных бага в продукте исправлены: TikTok Алины (`im_your_asya` открывал чужой аккаунт →
  `alinochka_vtb`, подтверждено Андреем напрямую) и TikTok Coffee (`best.cofee.show`, мёртвая
  опечатка → `best.coffee.show`, тоже подтверждено напрямую).
- Новый проект **Exclusive Man** добавлен в цепочку (@best_exlusive.man, была руководитель Марина
  Анирам, простаивает с 2021 года).
- Карточка «Релиз»/«Версия» теперь показывает честный цветной вердикт готовности сверху — не
  спрятан внутри списка пунктов, было впервые сделано в ходе подготовки к этому релизу.
- Презентация карточки «Релиз» переписана без технического жаргона — короткий связный рассказ
  вместо разрозненных фактов, по прямому слову Андрея «нужно думать, как показать это действительно
  презентационно».
- Остаются два отдельных открытых вопроса, не блокировавших выпуск 4.1, но ожидающих настоящего
  Релиза 5: новое имя для Valmont, лучший световой элемент для тора BestOfficial.
- Проверено на живом проде после деплоя: `https://bestofficial.vercel.app` отвечает (код 200),
  показывает «Релиз 4.1», отдаёт исправленные `js/data-projects.js`/`js/data-participants.js`.

## Официальный релиз 4 — 07.08.2026

Публичная страница обновлена через `git push` в `github.com/bestdreambot/BestOfficial` (main, четыре
коммита подряд — [`6a81055`](https://github.com/bestdreambot/BestOfficial/commit/6a81055),
[`6671506`](https://github.com/bestdreambot/BestOfficial/commit/6671506),
[`0eedc09`](https://github.com/bestdreambot/BestOfficial/commit/0eedc09),
[`bc9bf58`](https://github.com/bestdreambot/BestOfficial/commit/bc9bf58)) — следом задеплоено на
Vercel через `vercel --prod` из `.deploy-bestofficial/`. Источник — рабочая мастерская
`freex/bestofficial/freexofficial.html` (локальный порт 2000).

- Каждая карточка (проект, звезда, а также сам «Релиз»/«Версия») теперь двухпанельная:
  «Презентация» слева — только подтверждённые факты, звёздными иконками; «Процесс» справа — 🟡 в
  работе / 🔴 нужно узнать (зелёное не повторяется, чтобы не дублировать).
- Появился медиум-тор — уменьшенная копия главного тора (через `drawImage()` с главного канваса,
  без отдельной системы частиц) вокруг фото звезды на её карточке; в карточках проектов — тот же
  медиум-тор без фото, с названием проекта, наложенным прямо на тор.
- Под главным тором — две кликабельные подписи «Релиз N» / «Версия N»: «Релиз» открывает публичную
  карточку (3🟢/3🟡/3🔴 — сделано / в работе / планы), «Версия» — то же самое, но только для
  локального адреса (`isLocalHost` проверка по hostname), на публичном проде кнопка не показывается.
  Обе подписи скрываются насовсем после первого перехода по цепочке (`activateProject()`) — их видно
  только на самом первом экране, до перезагрузки страницы.
- У звёзд появился статус фото в торе (`photoConfirmed`) — зелёным помечено только то, что звезда
  прислала сама (на релиз 4 — Галя и Алина), у остальных красным — «старое/случайное фото из архива,
  пришли более свежее».
- Хотфикс: `.card-body` получил `max-height`/`overflow-y:auto` — в Telegram Mini App (более короткий
  viewport, чем на телефоне в браузере) без этого нижняя часть карточки (Процесс) была физически
  недостижима, показывался только хвост Презентации.
- Хотфикс: центрирование подписи «Релиз» под тором докручено (`-14px` → `-38px`) — на Telegram-вьюпорте
  слово визуально уезжало от центра.
- Публичный текст: нигде не упоминается личность/тор самого Андрея или Марики (закрытая тема, см.
  правило приватности) — везде, где раньше могло стоять «Создатель», теперь «Андрей».
- Проверено на живом проде после деплоя: `https://bestofficial.vercel.app` отвечает (код 200), отдаёт
  `js/best-app.js` с новым кодом (`renderStatusCard` присутствует).

## Официальный релиз 3.0 — 06.08.2026

Публичная страница обновлена через `git push` в `github.com/bestdreambot/BestOfficial` (main,
коммит [`ff6991d`](https://github.com/bestdreambot/BestOfficial/commit/ff6991d), обычный
fast-forward поверх всей прежней истории релизов, без потери коммитов) — следом задеплоено на
Vercel через `vercel --prod` из `.deploy-bestofficial/`. Источник — рабочая мастерская
`freex/bestofficial/freexofficial.html` (локальный порт 2000), в этот раз впервые полностью
пересобранная как отдельный движок, а не правки поверх старого.

- Единый новый движок (`js/best-app.js`) заменил три старых файла (`app.js`+`torus.js`+
  `word-fx.js`) — тот же гранёный алмазный Ктор и металлическое слово, что в мастерской уровня
  1000 (`FreexOfficial/freexofficial.html`), обобщённые на любую из ~40 палитр `WORLDS`, а не
  жёстко зашитые в серебро/золото.
- Все пять почерков перехода (portal / dissolve / wipe / burst / spiral) сохранены как в релизе 2.0
  — переехали на новый движок без потери разнообразия.
- FOM2 (в новом движке — «mktor») теперь настоящее canvas-кольцо с гранями, подпись внутри —
  тем же металл-градиентным шрифтом (Orbitron 900), что и главное слово, только компактнее; раньше
  подпись была плоским текстом без градиента.
- mktor статичный — перерисовывается только при смене мира/звезды, не каждый кадр (меньше нагрузка).
- На главном экране (до первого клика по слову) mktor теперь не показывается вовсе — как и раньше
  задумывалось, первый кадр остаётся только Ктор + слово.
- Слово больше не показывается видимым текстом под фото звезды (дублировало подпись в mktor) —
  решение, уже принятое Создателем 01.08.2026, восстановлено после того как потерялось при
  пересборке движка.
- Порядок цепочки звёзд изменён: Алина теперь сразу после Гали (было — четвёртой).
- Telegram Mini App (tg.expand(), тактильный отклик, openLink для TikTok-ссылок, тёмная шапка) —
  перенесена без изменений из релиза 1.7/1.8/2.0.
- Проверено на живом проде после деплоя: `https://bestofficial.vercel.app` отвечает (код 200),
  отдаёт новый `js/best-app.js` (не старые `app.js`/`torus.js`), порядок звёзд подтверждён.

## Официальный релиз 2.0 — 01.08.2026

Публичная страница обновлена через `git push` в `github.com/bestdreambot/BestOfficial` (main,
коммит [`3374029`](https://github.com/bestdreambot/BestOfficial/commit/3374029)) — Vercel подтвердил
деплой (`state: success`). Источник — рабочая мастерская `freexofficial.html`, порт 8770, метка
мастерской после релиза — `r2.0 v2.01`.

- **FOM2** — два маленьких статичных кольца-«пыли» в нижних углах экрана (~1/5–1/6 главного кольца,
  не крутятся). Левое — цвет **текущего** проекта/звезды, фиксируется только при реальном переходе
  (не меняется от клика по самому главному кольцу). Правое — «Best», всегда золото, не меняется никогда.
- Клик по имени в FOM2 открывает общую карточку (маленькое фото + имя + бейджи роли + факты) поверх
  притемнённого тора — работает и для звёзд (участников), и для самого Best, и для любого проекта
  цепочки (руководитель ищется по данным участников).
- Убрана старая pill-кнопка «Участники» и текстовая кнопка «Best» — их место заняли FOM2.
  Убран нижний текстовый бейдж-дублёр роли под фото звезды.
- Скорость вращения главного тора снижена в разы — полный оборот теперь долгий и спокойный
  (было заметно быстрее).
- Подпись TikTok под кольцом переоформлена — по центру, внутри кольца, приглушённым шрифтом.
- Полировка Telegram Mini App (заголовок/фон, viewport, openLink, лёгкая вибрация) — перенесена без
  изменений из релиза 1.8.

## Официальный релиз 1.2 — 30.07.2026

Публичная страница обновлена напрямую через `npx vercel --prod` из `.deploy-bestofficial/` — без
GitHub-коммита (тот же способ, что и в релизе 1.1). Источник — рабочая мастерская `freexofficial.html`,
порт 8770.

- Цепочка проектов по клику на слово выросла до 26 шагов (была 5 на момент релиза 1.1): Best, Aleorix,
  Jelly, Valmont, Animix, Anibrox, Coffee, Detski, Dostar, Dream, Dualis, Exclusive Stars, Finger Dance,
  Fovela, Grand Show, ISR, Moon, Nerix, News, Past Legends, Tiretok, Tune, Vanila, Voice, Noira, Zaryum.
  Данные вынесены в таблицу `PROJECTS[]`, каждый шаг — свой цвет тора, свой переход (portal / dissolve /
  wipe / burst / spiral) и свой почерк входа слова, подобраны так, чтобы соседние шаги не повторялись.
- Написание «Valmint» исправлено на «Valmont» везде (слово, aria-label, ключ палитры) — прямое слово
  Создателя, TikTok-адрес `@best.valmont.man` не менялся.
- У части проектов подтверждён и показан адрес — TikTok (`@best.…`) или, где адрес не TikTok, обычная
  ссылка с коротким подписанным текстом вместо длинного URL (Noira и Zaryum ведут на плейлисты YouTube).
  Там, где адрес не подтверждён Создателем, ссылка не показывается вовсе.
- Arhiv убран из публичной цепочки полностью (по слову Создателя — «в официальной версии архив не
  интересует»), остаётся только в отдельном проекте freex.
- Для длинных названий («Exclusive Stars», «Past Legends», «Finger Dance», «Grand Show») размер шрифта
  сделан таким же, как у «BestOfficial», а не увеличенным — иначе слово не помещалось.
- Тор Zaryum перекрашен в розовый.
- Проверено: `https://bestofficial.vercel.app` — отвечает (код 200), `.devinfo` (метка релиза/версии)
  на публичной странице по-прежнему не показывается.

## Официальный релиз 1.1 — 29.07.2026, ночь

Отдельный протокол нумерации — не связан с релизами/версиями других проектов (freex и т.д.) и не
совпадает по смыслу со старой записью «Релиз 1.1» ниже (та была про чёрный цвет/5 новых цветов, другой
контекст, оставлена как есть для истории).

- Публичная страница обновлена напрямую через `npx vercel --prod` из `.deploy-bestofficial/` — **без
  GitHub-коммита** (старая привязка проекта `bestofficial-standard` в `.vercel/project.json` перестала
  работать — "проект удалён/передан/нет доступа"; перепривязано на существующий проект `bestofficial`
  в той же команде `bestbs-projects`). Источник изменений — рабочая мастерская `freexofficial.html`,
  порт 8770.
- Мир по умолчанию стал **серебряным** (палитра `freex`), а не золотым — клик по слову «BestOfficial»
  превращает его в «Best» и только тогда тор становится золотым (палитра `best`). Прямое решение
  Создателя, отменяет более раннюю запись v9 «мир по умолчанию — best (золото)».
- Слову добавлены: два маленьких блика-искры внутри букв, дыхание свечения (плавная пульсация каждые
  ~3.8с), два прохода блика (мягкий + резкий) для металлической зеркальности. Цвет самого слова
  (золотой градиент) не менялся.
- Номер релиза/версии по-прежнему не показывается на публичной странице — было временно нарушено
  (добавлена видимая подпись «1.1»), сразу исправлено по слову Создателя, задеплоено заново без неё.
- Проверено: `https://bestofficial.vercel.app` — отвечает (код 200), содержит новое поведение.

## Релиз 1.2 — 29.07.2026 (запись до перепривязки Vercel-проекта, см. выше)

Внешний вид не менялся — только внутренние улучшения, по разбору предложений GPT.

- [Расчёт цвета стал быстрее](https://github.com/freexbestofficial-web/BestOfficial/commit/fe7a106) — раньше на каждую из ~2200 частиц каждый кадр код заново разбирал цвет из текста; теперь палитра готовится один раз за кадр. Меньше нагрузка на телефон.
- [Слово теперь доступно с клавиатуры](https://github.com/freexbestofficial-web/BestOfficial/commit/fe7a106) — Tab + Enter/пробел меняют цвет, как и клик.
- Круг помечен как декоративный для программ чтения с экрана — озвучивается только слово.
- Источник: советы GPT в `.agents/pablikgpt.md`, отчёты Kilo и Grok проверены — новых применимых фактов не дали (Grok писал про старый чужой репозиторий, не про текущий BestOfficial).

## Релиз 1.1 — 29.07.2026

- [Убран чёрный цвет из круга](https://github.com/freexbestofficial-web/BestOfficial/commit/7196f3f) — миру «AI» (чёрный) больше не место в BestOfficial.
- [Добавлено 5 новых цветов кольца](https://github.com/freexbestofficial-web/BestOfficial/commit/7196f3f) — tomato, nebula, maroon, lavender, violet (подобраны по референсам Создателя).
- Всего цветов в цикле по клику: 8.
- Адрес не менялся: https://bestofficial.vercel.app — отвечает (код 200).

## Релиз 1 — 29.07.2026

- [Публичная страница загружена](https://github.com/freexbestofficial-web/BestOfficial/commit/f6342ff) — золотое слово BestOfficial + кольцо примерно из 2200 золотых частиц в непрерывном движении.
- Публичный адрес: https://bestofficial.vercel.app — открыт, отвечает (код 200).
- Репозиторий: https://github.com/freexbestofficial-web/BestOfficial — отдельный аккаунт
  freexbestofficial-web, без ботов.
- Клик по слову плавно меняет цвет кольца.
- Телефон: страница открывается в любом браузере; на iPhone доступно добавление на домашний
  экран через «Поделиться → На экран Домой». Отдельной иконки и офлайн-режима пока нет.
- Разработку и код ведёт только Клод. Другие ИИ читают факты и могут оставлять советы и
  пожелания — менять код не могут.
